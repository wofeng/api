<h2 id="schedule-a-meeting">Schedule a meeting</h2>

<p>Schedule a meeting from zoom room client</p>

<blockquote>
<p>Definition</p>
</blockquote>
<pre class="highlight shell tab-shell"><code>POST https://api.zoom.us/v2/rooms/<span class="o">{</span>roomId<span class="o">}</span>/meetings
</code></pre>
<blockquote>
<p>Example Request</p>
</blockquote>
<pre class="highlight shell tab-shell"><code><span class="gp">$ </span>curl https://api.zoom.us/v2/rooms/<span class="o">{</span>roomId<span class="o">}</span>/meetings <span class="se">\</span>
  -d <span class="s1">'{
  "jsonrpc": "2.0",
  "method": "schedule",
  "params": {
    "meeting_info": {
        "topic": "my meeting",
        "start_time": "2017-11-25T12:00:00",
        "timezone": "America/Los_Angeles",
        "duration": "10"
    }
  }
}'</span>
</code></pre>
<blockquote>
<p>Example Response</p>
</blockquote>
<pre class="highlight plaintext"><code>200 OK
</code></pre><pre class="highlight json tab-json"><code><span class="p">{</span><span class="w">
  </span><span class="s2">"jsonrpc"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2.0"</span><span class="p">,</span><span class="w">
  </span><span class="s2">"result"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="s2">"room_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"63UtYMhSQZaBRPCNRXrD8A"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"send_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2017-09-15T01:26:05Z"</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="s2">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"49cf01a4-517e-4a49-b4d6-07237c38b749"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre>
<table><thead>
<tr>
<th>Path Arguments</th>
<th>&nbsp;</th>
</tr>
</thead><tbody>
<tr>
<td>roomId <strong>required</strong></td>
<td>The id of zoom room</td>
</tr>
</tbody></table>

<table><thead>
<tr>
<th>Body Arguments</th>
<th>&nbsp;</th>
</tr>
</thead><tbody>
<tr>
<td>jsonrpc</td>
<td>A String specifying the version of the JSON-RPC protocol.</td>
</tr>
<tr>
<td>method <strong>required</strong></td>
<td>The name of scheduling a meeting method to be invoked.</td>
</tr>
<tr>
<td>params.meeting_info.topic <strong>required</strong></td>
<td>Meeting topic. Max of 200 characters.</td>
</tr>
<tr>
<td>params.meeting_info.start_time <strong>required</strong></td>
<td>Meeting start time can be UTC time, such as 2017-11-25T12:00:00Z. A time zone offset is required unless a time zone is explicitly specified in <strong>timezone</strong> such as 2017-11-25T12:00:00, timezone is &ldquo;America/Los_Angeles&rdquo;.</td>
</tr>
<tr>
<td>params.meeting_info.timezone</td>
<td>Time zone is required if start time without time zone offset. it looks like America/Los_Angeles.</td>
</tr>
<tr>
<td>params.meeting_info.duration <strong>required</strong></td>
<td>Meeting duration of minutes</td>
</tr>
<tr>
<td>params.password</td>
<td>Meeting password. Password may only contain the following characters: [a-z A-Z 0-9 @ - _ *]. Max of <strong style="color: red">10</strong> characters.</td>
</tr>
<tr>
<td>params.callback_url</td>
<td>Provide a callback url if need to get the meeting number for the meeting. Send a post request with json payload using the callback url once zoom room client completed join meeting request. Callback url looks like this - https://api.zoom.us/callback?token=test123, Context-Type is application/json, the payload is <br/>{&ldquo;request_id&rdquo;: &ldquo;49cf01a4-517e-4a49-b4d6-07237c38b749&rdquo;, &ldquo;meeting_number&rdquo;:&ldquo;1234567890&rdquo;}.</td>
</tr>
<tr>
<td>params.meeting_info.settings.join_before_host</td>
<td>Join meeting before host start the meeting.</td>
</tr>
</tbody></table>
